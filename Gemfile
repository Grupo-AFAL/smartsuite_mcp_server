source 'https://rubygems.org'

ruby '>= 3.0.0'

# SQLite for caching layer
gem 'sqlite3'

# The server uses:
# - json (stdlib)
# - net/http (stdlib)
# - uri (stdlib)
# - time (stdlib)
# - fileutils (stdlib)
# - digest (stdlib)

group :test do
  gem 'minitest'
  gem 'rake'
end
