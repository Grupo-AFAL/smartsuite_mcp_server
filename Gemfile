source 'https://rubygems.org'

ruby '>= 3.0.0'

# No additional gems required - using Ruby standard library only
# The server uses:
# - json (stdlib)
# - net/http (stdlib)
# - uri (stdlib)
# - time (stdlib)
# - fileutils (stdlib)
# - digest (stdlib)

group :test do
  gem 'minitest'
  gem 'rake'
end
